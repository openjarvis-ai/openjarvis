"use client";

import { useState, useEffect } from "react";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { cn } from "@/lib/utils";
import {
  Video,
  GitBranch,
  LayoutDashboard,
  Images,
  Monitor,
  Zap,
  ChevronRight,
  ChevronLeft,
  X,
} from "lucide-react";

const ONBOARDING_STORAGE_KEY = "openjarvis-onboarding-dismissed";

const steps = [
  {
    title: "Welcome to OpenJarvis",
    subtitle: "Your screen recording & workflow review platform",
    description:
      "OpenJarvis helps you record your screen, extract key moments as screenshots, and share feedback with Opus. Collaborate seamlessly on workflows generated by OpenJarvis and Opus.",
    icon: Zap,
    gradient: "from-amber-500 to-orange-600",
  },
  {
    title: "Record Your Screen",
    subtitle: "Flexible capture options",
    description:
      "Record your entire screen, a specific application window, or a single browser tab. Pause and resume anytime, then download or upload your recording. All recordings are saved as WebM.",
    icon: Video,
    features: ["Entire screen, window, or tab", "Pause / resume / stop", "Download or upload"],
  },
  {
    title: "Extract & Share Screenshots",
    subtitle: "One frame per 10 seconds",
    description:
      "Automatically extract one screenshot for every 10 seconds of your recording, then send them to Opus with optional workflow and recording IDs.",
    icon: Images,
    features: ["1 frame per 10 seconds", "Send to Opus"],
  },
  {
    title: "Review Workflows",
    subtitle: "Browse and comment",
    description:
      "Explore workflows generated by OpenJarvis and Opus. View step-by-step details, status, and metadata. Send text comments directly to Opus for any workflow.",
    icon: GitBranch,
    features: ["Workflow list & details", "Step status timeline", "Comments to Opus"],
  },
  {
    title: "Dashboard Overview",
    subtitle: "Stay on top of activity",
    description:
      "Your home base: total recordings, workflows, comments sent, and total duration. See recent activity and jump straight into a new recording.",
    icon: LayoutDashboard,
    features: ["Stats at a glance", "Recent activity feed", "Quick new recording"],
  },
];

export function OnboardingOverlay() {
  const pathname = usePathname();
  const [isVisible, setIsVisible] = useState(false);
  const [currentStep, setCurrentStep] = useState(0);
  const [mounted, setMounted] = useState(false);

  useEffect(() => {
    setMounted(true);
  }, []);

  useEffect(() => {
    if (!mounted || pathname !== "/dashboard") return;
    try {
      const dismissed = localStorage.getItem(ONBOARDING_STORAGE_KEY);
      if (!dismissed) setIsVisible(true);
    } catch {
      setIsVisible(false);
    }
  }, [mounted, pathname]);

  const dismiss = () => {
    try {
      localStorage.setItem(ONBOARDING_STORAGE_KEY, "true");
      setIsVisible(false);
    } catch {
      setIsVisible(false);
    }
  };

  const next = () => {
    if (currentStep < steps.length - 1) {
      setCurrentStep(currentStep + 1);
    } else {
      dismiss();
    }
  };

  const prev = () => {
    if (currentStep > 0) setCurrentStep(currentStep - 1);
  };

  if (!mounted || !isVisible) return null;

  const step = steps[currentStep];
  const isLast = currentStep === steps.length - 1;
  const isFirst = currentStep === 0;
  const Icon = step.icon;

  return (
    <div
      className="fixed inset-0 z-[100] flex items-center justify-center bg-black/50 backdrop-blur-md p-4 sm:p-6"
      role="dialog"
      aria-modal="true"
      aria-labelledby="onboarding-title"
      aria-describedby="onboarding-description"
    >
      <div className="w-full max-w-lg max-h-[90vh] overflow-hidden flex flex-col rounded-2xl border border-surface-200/80 dark:border-surface-700/80 bg-white dark:bg-surface-900 shadow-2xl animate-fade-in">
        {/* Header */}
        <div className="flex items-center justify-between px-6 py-5 border-b border-surface-200/80 dark:border-surface-700/80">
          <div className="flex items-center gap-3">
            <div className="w-10 h-10 rounded-xl flex items-center justify-center bg-gradient-to-br from-brand-500 to-brand-700 shadow-sm">
              <Zap className="w-5 h-5 text-white" />
            </div>
            <span className="font-display text-lg font-medium text-surface-900 dark:text-surface-100">
              OpenJarvis
            </span>
          </div>
          <button
            type="button"
            onClick={dismiss}
            className="p-2.5 rounded-xl text-surface-500 hover:text-surface-700 hover:bg-surface-100 dark:hover:bg-surface-800 dark:hover:text-surface-300 transition-colors"
            aria-label="Skip onboarding"
          >
            <X className="w-5 h-5" />
          </button>
        </div>

        {/* Content */}
        <div className="flex-1 overflow-y-auto p-8 space-y-6">
          <div
            className={cn(
              "w-16 h-16 rounded-2xl flex items-center justify-center shadow-sm",
              step.gradient ? `bg-gradient-to-br ${step.gradient}` : "bg-brand-50 dark:bg-brand-900/30"
            )}
          >
            <Icon
              className={cn(
                "w-8 h-8",
                step.gradient ? "text-white" : "text-brand-600 dark:text-brand-400"
              )}
            />
          </div>
          <div>
            <h2 id="onboarding-title" className="text-2xl font-semibold text-surface-900 dark:text-surface-100 tracking-tight">
              {step.title}
            </h2>
            <p className="text-sm text-brand-600 dark:text-brand-400 font-medium mt-1.5">
              {step.subtitle}
            </p>
          </div>
          <p id="onboarding-description" className="text-sm text-surface-600 dark:text-surface-400 leading-relaxed">
            {step.description}
          </p>
          {step.features && (
            <ul className="space-y-3">
              {step.features.map((f, i) => (
                <li key={i} className="flex items-center gap-3 text-sm text-surface-600 dark:text-surface-400">
                  <span className="w-2 h-2 rounded-full bg-brand-500 flex-shrink-0" />
                  <span>{f}</span>
                </li>
              ))}
            </ul>
          )}
        </div>

        {/* Footer */}
        <div className="px-6 py-5 flex flex-col sm:flex-row items-center justify-between gap-4 border-t border-surface-200/80 dark:border-surface-700/80 bg-surface-50/50 dark:bg-surface-800/30">
          <div className="flex items-center gap-2">
            {steps.map((_, i) => (
              <button
                key={i}
                type="button"
                onClick={() => setCurrentStep(i)}
                className={cn(
                  "h-2 rounded-full transition-all duration-300",
                  i === currentStep
                    ? "w-6 bg-brand-600"
                    : "w-2 bg-surface-300 dark:bg-surface-600 hover:bg-surface-400 dark:hover:bg-surface-500"
                )}
                aria-label={`Go to step ${i + 1}`}
                aria-current={i === currentStep ? "step" : undefined}
              />
            ))}
          </div>
          <div className="flex items-center gap-3 w-full sm:w-auto">
            {!isFirst && (
              <button
                type="button"
                onClick={prev}
                className="btn-secondary px-4 py-2.5 rounded-xl"
              >
                <ChevronLeft className="w-4 h-4" />
                Back
              </button>
            )}
            {isLast ? (
              <Link
                href="/dashboard"
                onClick={dismiss}
                className="btn-primary px-5 py-2.5 rounded-xl font-medium flex-1 sm:flex-initial justify-center"
              >
                Get Started
                <Monitor className="w-4 h-4" />
              </Link>
            ) : (
              <button type="button" onClick={next} className="btn-primary px-5 py-2.5 rounded-xl font-medium flex-1 sm:flex-initial">
                Next
                <ChevronRight className="w-4 h-4" />
              </button>
            )}
          </div>
        </div>
      </div>
    </div>
  );
}
